import { Formbricks } from "./formbricks";
import { init, logout, setAttribute, setAttributes, setLanguage, setUserId, track } from "./lib";

export default Formbricks;

export { track, init, logout, setAttribute, setAttributes, setLanguage, setUserId };
