type: string
enum:
  - veryNegative
  - negative
  - neutral
  - positive
  - veryPositive
  - mixed
description: >-
  Sentiment label inferred from `valueText`. Read-only; absent until the record is enriched.
  `mixed` means positive and negative sentiment were both present rather than averaging to neutral.
