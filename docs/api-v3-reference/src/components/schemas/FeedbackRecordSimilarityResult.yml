type: object
additionalProperties: false
required:
  - data
  - meta
properties:
  data:
    type: array
    maxItems: 100
    items:
      $ref: ./FeedbackRecordSimilarityItem.yml
    description: Matches ordered by descending `score`.
  meta:
    $ref: ./FeedbackRecordSimilarityMeta.yml
