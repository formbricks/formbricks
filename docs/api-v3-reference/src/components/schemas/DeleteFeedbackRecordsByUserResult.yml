type: object
additionalProperties: false
required:
  - deleted_count
  - message
properties:
  deleted_count:
    type: integer
    minimum: 0
    description: Number of records deleted. `0` when the user had no remaining records — the operation is idempotent.
  message:
    type: string
    description: Human-readable status message.
