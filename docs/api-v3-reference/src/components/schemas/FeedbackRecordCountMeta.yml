allOf:
  - $ref: ./FeedbackDatasetContext.yml
unevaluatedProperties: false
description: >-
  The `meta` for the feedback-record count. Carries no pagination: a count is one number, not a page,
  so it composes the dataset context alone.
