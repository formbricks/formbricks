oneOf:
  - $ref: ./CreateSurveyNumberVariable.yml
  - $ref: ./CreateSurveyTextVariable.yml
description: >
  Survey variable accepted by `POST /api/v3/surveys`. `id` may be omitted and
  will be

  generated by the server. Provide an explicit cuid2 id when logic in the same
  request needs

  to reference this variable.
