allOf:
  - $ref: ./ListPaginationMeta.yml
  - $ref: ./FeedbackDatasetContext.yml
unevaluatedProperties: false
description: The list `meta` for feedback records — pagination plus which dataset answered.
