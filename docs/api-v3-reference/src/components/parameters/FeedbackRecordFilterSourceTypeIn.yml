in: query
name: filter[sourceType][in]
style: form
explode: true
schema:
  type: array
  maxItems: 100
  items:
    type: string
    maxLength: 255
description: Records from any of these source types, e.g. `survey`, `review`.
