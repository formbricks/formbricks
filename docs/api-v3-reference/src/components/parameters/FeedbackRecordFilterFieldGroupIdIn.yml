in: query
name: filter[fieldGroupId][in]
style: form
explode: true
schema:
  type: array
  maxItems: 100
  items:
    type: string
    maxLength: 255
description: Records in any of these field groups (ranking, matrix and grid questions).
