in: query
name: filter[createdAt][lte]
schema:
  type: string
  format: date-time
description: >-
  Only records created at or before this instant (**inclusive**). Mutually exclusive with
  `filter[createdAt][lt]` — sending both is a **400**.
