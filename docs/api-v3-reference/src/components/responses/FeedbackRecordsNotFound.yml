description: >-
  Not Found from the feedback store (`application/problem+json`, `code: not_found`). The record
  exists — the gateway has already authorized against its dataset — but has no embedding for the
  current model: it has no text, or was created moments ago and is not embedded yet.
content:
  application/problem+json:
    schema:
      $ref: ../schemas/HubProblem.yml
