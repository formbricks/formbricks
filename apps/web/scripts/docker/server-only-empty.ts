// The TypeScript entry points retain `server-only` imports for Next.js boundary enforcement. This
// dedicated Node bundle is itself a server-only artifact, so the marker is intentionally empty here.
export {};
