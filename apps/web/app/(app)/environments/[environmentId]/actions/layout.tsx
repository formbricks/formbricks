import { InnerContentWrapper } from "@formbricks/ui/InnerContentWrapper";

export default function ActionsAndAttributesLayout({ children }) {
  return <InnerContentWrapper pageTitle="Actions">{children}</InnerContentWrapper>;
}
