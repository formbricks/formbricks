import { MdxImage } from "@/components/MdxImage";
import ScheduleCallPreviewDate from "./images/schedule-call-preview-date.webp";
import ScheduleCallPreviewTime from "./images/schedule-call-preview-time.webp";
import ScheduleCallPreview from "./images/schedule-call-preview.webp";
import ScheduleCall from "./images/schedule-call.webp";

#### Question Type

# Schedule A Meeting

The Schedule A Meeting question type allows respondents to book a meeting by selecting a date and time. It includes a title to guide the respondent, along with an optional description to provide additional context for the meeting setup.

<MdxImage
  src={ScheduleCall}
  alt="Overview of Schedule question type"
  quality="100"
  className="max-w-full rounded-lg sm:max-w-3xl"
/>

# Configuration

## Options

1. **Title:** Add a title to explain the purpose of the meeting, such as "Book Your Meeting Slot."
2. **Description:** Provide an optional description, such as "Please select a convenient date and time for our meeting."
3. **Cal.com Username/Event:** Add an input box where users can enter their [`cal.com`](https://cal.com/) username and event URL (e.g., `username/event`).
4. **Custom Hostname (Optional):** Enable an input box for adding a custom hostname, which is necessary if using a self-hosted instance of [`cal.com`](https://cal.com/docs/self-hosting/installation).

# Preview

<MdxImage
  src={ScheduleCallPreview}
  alt="Preview of Schedule question type"
  quality="100"
  className="max-w-full rounded-lg sm:max-w-md"
/>

# Date Selection

<MdxImage
  src={ScheduleCallPreviewDate}
  alt="Preview of Schedule question type when date getting selected"
  quality="100"
  className="max-w-full rounded-lg sm:max-w-md"
/>

# Time Selection

<MdxImage
  src={ScheduleCallPreviewTime}
  alt="Preview of Schedule question type when time getting selected"
  quality="100"
  className="max-w-full rounded-lg sm:max-w-md"
/>
